# Authentication for npm registry (for publishing and private packages)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# GitHub Packages registry for @trafficbyintent scope
@trafficbyintent:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Note: @trafficbyintent/kysely-bigquery will be published to npm registry as a public package
# The publishConfig in package.json overrides any scope configurations