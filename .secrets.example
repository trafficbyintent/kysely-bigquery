# Local development and testing configuration
# DO NOT commit .secrets to version control!
#
# BigQuery credentials are for LOCAL testing only — they are NOT used in CI.
# NPM_TOKEN is used both locally and in CI (set as a GitHub secret for CI).

# Required for npm publishing and installing private @trafficbyintent packages
NPM_TOKEN=your-npm-automation-token-here

# BigQuery credentials for local integration tests (not used in CI)
GOOGLE_APPLICATION_CREDENTIALS=./.serviceAccount.json
GCP_PROJECT_ID=your-project-id
BIGQUERY_DATASET=test_dataset

# Alternative: inline credentials for local testing or act
BIGQUERY_CREDENTIALS={"type":"service_account",...entire JSON content...}
BIGQUERY_PROJECT_ID=your-project-id